## Getting Started

First, install dependencies:

```bash
pnpm install
```

run the code expample:

Node version `v20` is recommended for this example.

```bash
node index.js
```

After the code example finished, you will deploy a bucket manager.

Feel free to try other methods in the  `BucketManager`, `BucketRegistry`.